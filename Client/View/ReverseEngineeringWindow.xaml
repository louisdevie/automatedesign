<Window x:Class="AutomateDesign.Client.View.ReverseEngineeringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:my="clr-namespace:AutomateDesign.Client.View.Controls"
        mc:Ignorable="d"
        Title="Rétro-génération" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0"
               Style="{StaticResource InputLabel}">
            Dossier contenant l'automate
        </Label>
        <my:DirectorySelectionInput Grid.Row="1"/>
        
        <Label Grid.Row="2" Style="{StaticResource InputLabel}"
               Margin="0 20 0 0">
            Éléments à importer
        </Label>

    </Grid>
</Window>
